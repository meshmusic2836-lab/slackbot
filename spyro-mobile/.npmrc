# Expo projects routinely have transitive peer-dep mismatches (RN's peer
# ranges lag behind). This is the officially recommended npm flag for Expo.
# See https://docs.expo.dev/troubleshooting/known-issues/#npm-peer-dependency-conflicts
legacy-peer-deps=true
